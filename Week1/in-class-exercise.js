var x = 0;
var doublex = 0;

function timesTwo(num) {
    return num * 2;
}

var x = 15;
var doublex = timesTwo(x);

var numbers = 0;

numbers = [x, doublex];

for (var i = 0; i > numbers.length; i++) {
    console.log(numbers);
    return numbers = {};
}

numbers = {
    y: doublex;
}
